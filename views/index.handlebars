<h1>Eat Da Burger</h1>

<h2>What's on the menu</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{burger_name}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Yummy all done</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
     {{burger_name}}
    {{/if}}
  {{/each}}
</ul>